services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: helpline
      POSTGRES_PASSWORD: helpline_secret
      POSTGRES_DB: helpline
    ports:
      - "5444:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
